---
output: blastula::blastula_email
params: 
  dois: NULL
  cr_overview: NULL
  session_id: NULL
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = FALSE, warning = FALSE, message = FALSE)
library(metacheck)
```

Hallo,

vielen Dank, dass Sie den Open-Access-Metadaten-Schnelltest nutzen.

Hier ist der Bericht der automatischen Überprüfung. 
Er bezieht sich auf `r length(unique(params$cr_overview$cr_overview$doi))` Publikationen, für die Metadaten in Crossref indexiert sind.

### Überblick: Crossref-Metadaten

```{r metrics_overview}
metrics_overview(params$cr_overview$cr_overview) %>%
   ind_table_to_gt(.color = "#00A4A7")
```


### Creative-Commons-Lizenzen

#### Überblick Lizenzbedingungen

```{r cc_metrics}
cc_metrics(params$cr_overview$cc_license_check) %>%
  ind_table_to_gt(.color =  "#F3A9BB")
```

#### Ergebnis Compliance Check

```{r cc_compliance}
cc_compliance_metrics(params$cr_overview$cc_license_check) %>%
  ind_table_to_gt(.color = "#A0A5A9")
```

Im beigefügten Excel-Spreadsheet dokumentiert das Blatt **CC-Lizenzen** je Artikel das Ergebnis der Überprüfung.

### Text and Data Mining (TDM)

```{r tdm_metrics}
tdm_metrics(params$cr_overview$tdm) %>%
  ind_table_to_gt(.color =  "#9B0056")
```

Die Tabelle bietet einen Überblick, welche Dateiformate,
ausgedrückt als [MIME
Type](https://developer.mozilla.org/en-US/docs/Web/HTTP/Basics_of_HTTP/MIME_types/Common_types),
für das Text- und Data-Mining (TDM) unterstützt werden. Grundsätzlich
kann je Artikel mehr als ein Format über das Crossref-Metadatenprofil
angeboten werden.

Der prozentuale Anteil bezieht sich auf sämtliche in Crossref-indexierte Artikel.

Im beigefügten Excel-Spreadsheet dokumentiert das Blatt **TDM** die Crossref-Metadaten über Möglichkeiten für das Text und Data Mining je Artikel.

### Förderinformationen 

```{r funder_info}
funder_metrics(params$cr_overview$funder_info) %>%
  ind_table_to_gt(.color = "#000099")
```

Die Tabelle gibt einen Überblick, über die häufigsten Förderer je Artikel lt. Crossref-Metadaten. 

Der prozentuale Anteil bezieht sich auf sämtliche in Crossref-indexierte Artikel.

Im beigefügten Excel-Spreadsheet dokumentiert das Blatt **Förderinformationen** das *Funding Acknowledgement* je Artikel lt. Crossref.

Falls Sie weitere Informationen benötigen, wie das Ergebnis des Schnelltests zu interpretieren ist, finden Sie auf unserer [Webseite](https://subugoe.github.io/metacheck/articles/methode.html). 

Bei Fragen und Anregungen stehen wir Ihnen gerne unter metacheck-support@sub.uni-goettingen.de zur Verfügung.

Viele Grüße

HOAD-Team

```{r export}
md_data_attachment(params$cr_overview, session_id = params$session_id, dois = params$dois)
```
